{
  "name": "buildora",
  "version": "0.1.0",
  "private": true,
  "description": "Buildora \u2014 Bangladesh construction super-platform (monorepo)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write .",
    "test": "turbo run test"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "turbo": "^2.10.2"
  }
}
